//
// Created by cyc on 18-12-8.
//

#include "dijkstra.h"

void Dijkstra(Graph G, int v) {
    int distance[MAX_VERTEX_NUM]; //length og i to j
    int pred[MAX_VERTEX_NUM];  //finded vertex
    int visit[MAX_VERTEX_NUM];
    int cost
    for (int i = 0; i < MAX_VERTEX_NUM; ++i) {
        visit[i] = 0;
        for (int j = 0; j < MAX_VERTEX_NUM; ++j) {
            if(G.arcs[i][j].adj == 0)

        }
        distance[i] =

    }

}